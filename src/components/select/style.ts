import { StyleSheet } from "react-native"

const selectStyles = StyleSheet.create({
    container: {
    backgroundColor: '#eee', 
    borderRadius: 20, 
    overflow:'hidden'

    }
})

export default selectStyles;